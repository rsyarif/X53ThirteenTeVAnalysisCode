#include "TFile.h"
#include "TH1.h"
#include "TLegend.h"
#include <vector>
#include <stdio>
#include <iostream>

void cmid_comparison(){


  std::vector<TString> eras; eras.push_back("2016B");eras.push_back("2016C");eras.push_back("2016D");eras.push_back("2016E");eras.push_back("2016F");eras.push_back("2016G");eras.push_back("2016H2");eras.push_back("2016H3");eras.push_back("All");
  //draw low pt
  TCanvas* c_lpt = new TCanvas();
  TLegend* leg_lpt = new TLegend(0.1,0.5,0.4,0.9);
  leg_lpt->SetFillStyle(0);
  leg_lpt->SetBorderSize(0);
  for(unsigned int i=0; i < eras.size(); i++){
    TFile* f_lpt = new TFile("ChargeMisID_Data_"+eras.at(l)+"_Electrons_MVATightRC_corrected.root");
    TH1F* h_lpt = (TH1F*) f_lpt->Get("h_num_lpt");
    
  }

}
